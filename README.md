# Example API
Simple base code for AI reverse proxies.

## Dependencies
- Python 3.8+
- MongoDB database
- A fucking brain

## How to Run
```bash
pip install -r requirements.txt -U
chmod +x run.sh
./run.sh
```